<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GridLayout" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GridLayout" parent="Base.Theme.GridLayout" />
    <style name="imageDesc">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/md</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="imageIcon">
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_width">@dimen/imgS</item>
        <item name="android:layout_height">@dimen/imgS</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="cardView">
        <item name="cornerRadius">15dp</item>
        <item name="cardElevation">15dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>